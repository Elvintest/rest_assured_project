{
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "внутренний идентификатор"
    },
    "regNumber": {
      "type": "string",
      "description": "регистрационный номер записи ЖУВД"
    },
    "sourceSystem": {
      "type": "string",
      "description": "код системы источника входящего документа"
    },
    "deliveryChannel": {
      "type": "string",
      "description": "код способа доставки входящего документа"
    },
    "document": {
      "$ref": "DocumentInfo.json"
    },
    "createdAt": {
      "type": "string",
      "description": "дата-время создания записи",
      "format": "date-time"
    },
    "result": {
      "$ref": "ResultInfo.json"
    }
  },
  "description": "Запись ЖУВД"
}